package com.example.pdfoverlay.ui;

/**
 * Herramientas disponibles en el editor visual.
 */
public enum EditorTool {
    SELECT,
    TEXT_FIELD,
    LABEL,
    BUTTON,
    MARKER,
    TABLE,
    MEASURE
}
